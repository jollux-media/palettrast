## Shared environment template (production/staging/local).
## Keep secrets out of git. Use `.env.local` for local development.

DATABASE_URL=
CLERK_SECRET_KEY=
VITE_CLERK_PUBLISHABLE_KEY=
# Optional Clerk Account Portal URLs (fallback if auth UI fails to load)
CLERK_SIGN_IN_URL=
CLERK_SIGN_UP_URL=
